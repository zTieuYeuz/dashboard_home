# Claude Code worktrees & local settings
.claude/

# Wrangler local cache
.wrangler/

# Node modules (nếu sau này thêm)
node_modules/

# Env files / local secrets — never commit
.env
.env.local
.dev.vars
.dev.vars.*
*.secret
import-secrets.ps1
sp_tmp.json
dev_tmp.json

# Technical notes — contains sensitive info (IPs, URLs, credentials)
NOTES.md
# Test & docs không liên quan
TEST_PLAN.md
TEST_PLAN_Movi_Dashboard.xlsx
generate_test_xlsx.py
HANDOVER.md
GIT_BRANCHING_GUIDE.md
wrangler.staging.toml

# n8n workflow JSONs — chứa credentials ESXi, không commit
n8n-workflows/
frigate-config.yml
frigate-config-ai.yml
RUSTDESK-GUIDE.md

# ── Files hạ tầng KHÔNG cần cho Cloudflare build ──
# Cloudflare chỉ build từ: worker.js + public/ + wrangler.toml
# Các file dưới vẫn giữ local + trong git history; ẩn khỏi GitHub để repo gọn + không lộ thông tin nội bộ
docker/
n8n/
docs/
scrypted-docker-compose.yaml

